import { Box, Typography } from '@strapi/design-system';

const Settings = () => {
  return (
    <Box padding={8}>
      <Typography variant="alpha">
        Strapi Localize Settings
      </Typography>
      <Typography>
        Settings page is working!
      </Typography>
    </Box>
  );
};

export default Settings;
